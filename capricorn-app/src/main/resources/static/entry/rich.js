'use strict';

(function ($, root, component) {

    var proto = {

    };

    $.extend(proto, {
    });

    $(function () {
        proto.initialize();
    });

    root[component] = exporter(proto, function() {

    });

})(jQuery, window, 'rich');
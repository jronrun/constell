'use strict'

;(function($, component) {

    var root = Function('return this')();

    var proto = {

    };

    root[component] = exporter(proto, function() {

    });

}.call(this, jQuery, 'quills'));